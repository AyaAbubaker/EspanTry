# ESPAN — إعداد الأمان قبل النشر العام

هذه النسخة تستخدم جلسة داخل **HttpOnly Cookie** بدل تخزين Token في localStorage، وتخزن Hash للجلسة فقط في SQLite. كما تستخدم SameSite=Strict، Security Headers، تحديد محاولات الدخول/التسجيل/الاسترجاع، PBKDF2-SHA256 بعدد 600,000 دورة، وصلاحيات Backend حسب الدور.

## عند النشر على دومين

1. شغّل `start-production.sh` خلف Caddy أو Nginx فقط.
2. يجب أن يكون الموقع الخارجي HTTPS. وضع Production يفعّل `Secure` على Cookie وHSTS.
3. لا تفتح منفذ Python 3000 للعامة؛ اجعله متاحًا محليًا فقط `127.0.0.1`.
4. غيّر كلمة مرور المدير إلى كلمة قوية ومختلفة عن رقم الهاتف بعد التسليم.
5. شغّل `backup.sh` دوريًا واحفظ النسخ الاحتياطية خارج نفس الخادم.
6. حدّث نظام التشغيل وPython وReverse Proxy بصورة دورية.
7. احصر صلاحية الوصول إلى ملفات `data/` و`backups/` بحساب تشغيل الخدمة فقط.

## ملاحظات

- رموز Recovery Code محفوظة كـHash فقط، والرمز القديم يُلغى عند الاسترجاع.
- تغيير كلمة المرور يلغي الجلسات السابقة.
- الاستيراد المباشر وReset Demo غير متاحين في هذه النسخة.
- Rate limiting الداخلي مناسب لخادم واحد؛ عند حركة عالية يفضّل إضافة Rate Limit أيضًا في Reverse Proxy.
